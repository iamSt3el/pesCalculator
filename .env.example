# Neon pooled connection string
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
# Tests DROP SCHEMA on every run, so point them at a throwaway database
TEST_DATABASE_URL=postgresql://user:password@host/dbname_test

# openssl rand -hex 32
SESSION_SECRET=replace-me
PORT=3000
NODE_ENV=development
